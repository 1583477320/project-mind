---
name: project-memory
description: 项目级记忆系统。在打开项目/会话启动时自动检查并加载项目记忆（.reasonix/memory/ 下的 index.md 与 progress.md）；项目尚无记忆时，根据项目内文档自动创建记忆（知识库 knowledge.md + 进度 progress.md + 索引 index.md）。当用户要求"初始化记忆/更新记忆/查看记忆"、要求记住或查询项目的术语/架构/约定/进度、或一轮任务/对话收尾需要写回进度与决策时使用。也用于打开一个陌生项目时快速建立对项目的理解。
---

# Project Memory（项目记忆系统）

## 定位

为当前项目维护一份"知识 + 进度"记忆，数据存放在项目内 `.reasonix/memory/`，随项目走、可纳入 git：

| 文件 | 内容 | 更新频率 |
|---|---|---|
| `knowledge.md` | 静态知识：术语表、架构、技术选型、约定、文档摘要 | 低 |
| `progress.md` | 动态进度：已完成、进行中、下一步、更新记录 | 高（每次任务结束） |
| `index.md` | 索引：模块 → 关键词 → knowledge.md 章节位置 | 随 knowledge 更新 |

## 消费（会话启动时执行）

打开项目开始对话时：

1. 检查 `.reasonix/memory/index.md` 是否存在：
   - **存在** → 读入 `index.md` 与 `progress.md`（都很短），`knowledge.md` 不整读，按需查（用 index 定位章节后只读对应小节）；
   - **不存在** → 执行下面的"初始化流程"自动创建记忆，然后读入。
2. 用户询问项目情况时，先查记忆再查源文档，避免重复理解。

## 初始化流程

触发：会话启动检测到无记忆、或用户说"初始化记忆"。

1. 扫描项目文档：`README*`、`docs/**/*.md`、主源文件（如 `main.tex`）、参考文献（`*.bib`）；大项目按文件大小/重要性分级，优先扫 README、docs、主文件。
2. 从文档提取四类内容：
   - **术语表**：项目特有名词 + 一句话解释（含缩写、公式记号）；
   - **架构与模块**：项目结构、主要模块与职责、文件之间关系；
   - **技术选型与约定**：工具链、写作/命名约定、关键决策；
   - **文档摘要**：每个关键文档 1-2 句话。
3. 写入 `knowledge.md`（按"格式约定"的模板），同时生成 `index.md`。
4. 生成 `progress.md` 骨架（已完成 / 进行中 / 下一步 / 更新记录），"进行中"填"初始化记忆"。
5. 向用户报告已创建记忆及记忆文件路径。

## 手动更新

触发：用户说"更新记忆"。

1. 重新扫描项目文档（同初始化步骤 1-2）。
2. 与旧记忆逐节 diff：
   - **新增内容** → 追加到对应章节；
   - **内容变化** → 更新对应小节；
   - **消失内容** → 若条目带 `<!-- 人工确认 -->` 注释，先询问用户再删；否则直接删除。
3. 只修改受影响的文件（知识变化动 `knowledge.md` 并同步 `index.md`，进度变化动 `progress.md`）。
4. 向用户报告更新摘要（新增 / 修改 / 删除条目数）。

## 自动更新（两条约定，避免频繁全量扫描）

1. **任务结束写回**：每轮对话/任务收尾时，把本轮完成事项与决策追加进 `progress.md` 的"更新记录"（带日期条目），并同步"已完成/进行中/下一步"。
2. **文档变化检测**：比较项目文档的修改时间/内容哈希与记忆文件的记录；仅当有实质变化时，触发一次知识更新（等同手动更新流程），并更新 `index.md`。

## 格式约定

`knowledge.md`：

```markdown
# 项目知识
## 术语表
| 术语 | 解释 |
|---|---|
## 架构与模块
## 技术选型与约定
## 文档摘要
```

`progress.md`：

```markdown
# 项目进度
## 已完成
## 进行中
## 下一步
## 更新记录（倒序）
- YYYY-MM-DD：...
```

`index.md`：

```markdown
| 模块 | 关键词 | knowledge.md 章节 |
|---|---|---|
```

## 边界与合并规则

- 空项目 / 无文档：生成骨架并标注"待补充"，不报错。
- 大项目扫描超限：只扫 README、docs、主文件，其余标注"未扫描"。
- 合并冲突：人工标注条目（`<!-- 人工确认 -->`）优先于自动提取结果。
- 记忆文件损坏或格式异常：备份旧文件为 `*.bak` 后重建，不静默覆盖。
